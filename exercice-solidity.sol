// fist exercice with tutorial

pragma solidity >=0.8.2 <0.9.0;

// create contract that stores and return the data
// step: 1. receive information, 2. store information (data integers), 3. return

contract simpleStorage {
    uint storeData;
    string names;
    bool switchON;

}